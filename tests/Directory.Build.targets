<Project>
    <PropertyGroup Condition="'$(IsTestProject)' == 'true'">
        <!-- Tests run on xUnit's sync context; ConfigureAwait(false) is unnecessary and can break test semantics. -->
        <NoWarn>$(NoWarn);CA2007;MA0004</NoWarn>
    </PropertyGroup>
</Project>
